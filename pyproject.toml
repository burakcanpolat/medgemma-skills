[project]
name = "med-rehber"
version = "1.0.0"
description = "AI guide for understanding medical results in plain language"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "pydicom>=3.0",
    "numpy>=1.20",
    "Pillow>=9.0",
]

[tool.uv]
dev-dependencies = []
